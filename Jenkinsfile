pipeline {
    agent any

    stages {
        stage('maven version') {
            steps {
                echo 'mvn version'
            }
        }
         stage('Running clean test') {
            steps {
                echo 'mvn clean test'
            }
        }
    }
}
